1. Klasa realizująca circular buffer, albo po prostu sprzężenie. Mogę zrobić tak, że będę wywoływał
funkcje z dodawaniem/kopiowaniem buforów i będą miały np. tylko 2 argumenty, tylko te które zmieniam.
Czyli funkcja w funkcji?
2. Wszystkie filtry i obliczenia z nimi związane, tworzenie ich w jednej klasie.
3. Osobna klasa na zjawiska przestrzenne.
4. Np. jeśli będę robił zagęszczenie późnego ogona to obliczenia też mogą być w tej klasie. Może to być 
w tej samej klasie, co losowanie czasów linii opóźnienia.
5. Jeśli coś z szumem to też osobna klasa. 
6. Może też być klasa realizująca późny pogłos - mogą tam być wykonywane operacje na wykorzystując klasy
4 i 5 np. 
7. Pewnie będzie tak, że rzeczy będą od siebie zależeć: np. różna filtracja obu kanałów w zależności od
różnicy głośności obu kanałów lub ich opóźnienia. Może osobna klasa do obsługiwania tego, lub w inny
sposób. 
8. Całe processowanie audio może być robione w osobnej klasie (wtedy w processBlocku w klasie procesor 
po prostu będzie coś w stylu klasa.funkcja(buffer)). Czyli np. ReverbEngine.process(buffer).
A w funkcji prepare w processorze też będzie tylko np. ReverbEngine.prepare(sampleRate, samplesPerBlock)
Więc w przypadku tworzenia edytora to będzie : np. processor.ReverbEngine.FilterGenerator.lowPass =
= filterSlider.getValue();